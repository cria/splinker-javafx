name: Compilar e empacotar o spLinker para Windows e Linux

on:
  push:
    branches:
      - master

jobs:
  # build-on-linux:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v3

  #     - name: Configurar Java
  #       uses: actions/setup-java@v3
  #       with:
  #         java-version: 20
  #         distribution: 'temurin'
  #         architecture: x64

  #     - name: Maven local dependency
  #       run: mvn install:install-file -Dfile=".\libs\yajsync-app-0.9.0-SNAPSHOT-full.jar" -DgroupId="com.github.perlundq" -DartifactId="yajsync" -Dversion="0.9.0-SNAPSHOT" -Dpackaging="jar"

  #     - name: Maven install
  #       run: mvn install
        
  #     - name: Maven compile
  #       run: mvn compile

  #     - name: Maven Package
  #       run: mvn package
  
  #     - name: Cria image jpackage
  #       run: jpackage --type app-image --name spLinker --input target/ --main-jar splinker-1.0-SNAPSHOT.jar

  #     - name: Cria pasta do arquivo SQL
  #       run: mkdir ./splinker/scripts; mkdir ./splinker/scripts/sql;
      
  #     - name: Copia arquivo SQL
  #       run: cp ./scripts/sql/create_tables.sql ./splinker/scripts/sql

  #     - name: Empacotar para Linux
  #       run: jpackage --name spLinker --type deb --dest dist --linux-shortcut --app-image spLinker

  #     - name: Upload artifacts
  #       uses: actions/upload-artifact@v3
  #       with:
  #         name: artifacts
  #         path: dist

  build-on-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: Configurar Java
        uses: actions/setup-java@v3
        with:
          java-version: 20
          distribution: 'temurin'
          architecture: x64

      - name: Maven local dependency
        run: mvn install:install-file -Dfile=".\libs\yajsync-app-0.9.0-SNAPSHOT-full.jar" -DgroupId="com.github.perlundq" -DartifactId="yajsync" -Dversion="0.9.0-SNAPSHOT" -Dpackaging="jar"

      - name: Maven install
        run: mvn install
        
      - name: Maven compile
        run: mvn compile

      - name: Maven Package
        run: mvn package

      - name: Cria image jpackage
        run: jpackage --type app-image --name spLinker --input target/ --main-jar splinker-1.0-SNAPSHOT.jar

      - name: Cria pasta do arquivo SQL
        run: mkdir spLinker\scripts; mkdir spLinker\scripts\sql;
      
      - name: Copia arquivo SQL
        run: cp scripts\sql\create_tables.sql spLinker\scripts\sql 

      - name: Empacota para Windows
        run: jpackage --name spLinker --type msi --dest dist --win-shortcut --app-image spLinker
        #--java-options '-Djava.library.path=$APPDIR'

      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: artifacts
          path: dist